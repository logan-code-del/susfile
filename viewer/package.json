{
  "name": "susfile-viewer",
  "version": "0.1.0",
  "description": "Local Electron viewer for viewer.html that enforces a timer/password before allowing close.",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "dependencies": {
    "dotenv": "^16.1.4"
  },
  "devDependencies": {
    "electron": "^26.0.0"
  }
}
